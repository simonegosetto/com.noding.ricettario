export interface ModalConfig {
    title: string;
    cancelText?: string;
    confirmText?: string;
    data: any;
}

// export type ModalConfigOpen = Pick<ModalConfig, 'title' | 'cancelText' | 'confirmText'>
